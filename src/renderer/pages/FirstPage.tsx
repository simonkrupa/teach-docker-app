export default function FirstPage() {
  return (
    <div
      // className={isCollapsed ? 'full-width' : 'collapsed-width'}
      // style={{ display: 'flex' }}
      className="all-pages"
    >
      Hello
    </div>
  );
}
